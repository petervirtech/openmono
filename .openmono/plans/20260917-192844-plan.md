# Plan — 20260917-192844 UTC

Session: sess_5f2af89ef4a448b3

## Plan: Rename "Peter" to "Piet", make text bigger/blockier, add zoom scroll effects

### Changes to `/workspace/index.html`

1. **Rename all instances of "Peter" to "Piet"** (6 locations):
   - Line 6: `<title>Piet | Aspiring Developer</title>`
   - Line 317: `<div class="logo">Piet</div>`
   - Line 328: `<h1>Piet</h1>`
   - Line 338: "Hi, I'm Piet" in About section
   - Line 411: email anchor text `piet@example.com`
   - Line 417: footer copyright

2. **Make hero h1 and nav logo bigger and blockier**:
   - `.hero h1`: increase `font-size` from `4rem` to `6rem`, add `letter-spacing: 0.05em`, change `font-weight` to `900`, add `text-transform: uppercase` for a blockier look, increase `margin-bottom` to `1.5rem`.
   - `.nav .logo`: increase `font-size` from `1.25rem` to `1.75rem`, change `font-weight` to `800`, add `letter-spacing: 0.04em`.

3. **Zoom-in transition on hero section load / first view**:
   - Wrap the hero content (h1, tagline, cta-btn) in a container with class `.hero-content`.
   - Add CSS animation: start at `transform: scale(0.85); opacity: 0;` and animate to `transform: scale(1); opacity: 1;` over 1s with ease-out.

4. **Zoom-out on scroll away from hero section**:
   - Add JavaScript using `IntersectionObserver` on the `.hero` section.
   - When the hero is fully in view (threshold ~0.6+), apply a scale of 1 and full opacity (zoomed in).
   - When scrolling away and the hero moves out of view, scale down to ~0.85 and reduce opacity, creating a zoom-out effect.
   - Use `transform: scale()` and `opacity` on `.hero-content` driven by scroll position for smooth transitions.

### Files changed
- `/workspace/index.html` — all changes in-place (CSS + JS additions, HTML structure tweak)
